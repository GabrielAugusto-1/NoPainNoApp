import { NavigationContainer } from "@react-navigation/native";
import RootStack from "./app/(tabs)/index";

export default App(){
    return(
        <NavigationContainer>
            <RootStack/>
        </NavigationContainer>
    );
}

